import { CommonModule } from '@angular/common';
import * as i0 from '@angular/core';
import { ViewChild, Input, Component } from '@angular/core';
import * as i1 from '@angular/common/http';

class FNIconComponent {
    http;
    cdr;
    name; // Icon name without extension
    variant = 'Line';
    size = 'medium';
    color;
    strokeWidth; // optional - for line icons
    disabled = false;
    className = ''; // CSS class name for Tailwind support
    basePath = 'assets/icons'; // Base path for icon assets
    iconContainer;
    // Size mapping
    sizeMap = {
        extrasmall: 10,
        small: 16,
        medium: 20,
        large: 24,
        'x-large': 32,
        xxlarge: 48,
    };
    get iconSize() {
        return this.sizeMap[this.size];
    }
    constructor(http, cdr) {
        this.http = http;
        this.cdr = cdr;
    }
    getIconPath(icon, variant = 'Line', size = 24) {
        if (!icon)
            return null;
        // For size 16 or 10 → normal path
        if (size === 16 || size === 10) {
            return `${this.basePath}/${variant}/${size}px/${icon}.svg`;
        }
        // For 20, 24, ... → add `--{size}` suffix
        return `${this.basePath}/${variant}/${size}px/${icon}--${size}.svg`;
    }
    ngOnChanges() {
        if (!this.name) {
            this.clearSvg();
            return;
        }
        const iconPath = this.getIconPath(this.name, this.variant, this.iconSize);
        if (!iconPath) {
            this.clearSvg();
            return;
        }
        this.http.get(iconPath, { responseType: 'text' }).subscribe({
            next: (raw) => this.processSvgRaw(raw),
            error: () => {
                this.clearSvg();
                this.cdr.detectChanges();
            },
        });
    }
    processSvgRaw(raw) {
        try {
            const parser = new DOMParser();
            const doc = parser.parseFromString(raw, 'image/svg+xml');
            const svg = this.validateSvgDocument(doc);
            if (!svg) {
                this.clearSvg();
                return;
            }
            this.removeStyleElements(svg);
            this.normalizeColors(svg);
            this.configureSvgAttributes(svg);
            this.insertSvg(svg);
            this.cdr.detectChanges();
        }
        catch (error) {
            console.error(`[fn-icon] Error parsing SVG for icon "${this.name}":`, error);
            this.clearSvg();
            this.cdr.detectChanges();
        }
    }
    clearSvg() {
        if (this.iconContainer?.nativeElement) {
            this.iconContainer.nativeElement.innerHTML = '';
        }
    }
    insertSvg(svg) {
        this.clearSvg();
        if (this.iconContainer?.nativeElement) {
            this.iconContainer.nativeElement.appendChild(svg);
        }
    }
    validateSvgDocument(doc) {
        const parserError = doc.querySelector('parsererror');
        if (parserError) {
            return null;
        }
        const svg = doc.documentElement;
        return svg.tagName === 'svg' ? svg : null;
    }
    removeStyleElements(svg) {
        Array.from(svg.querySelectorAll('style')).forEach((s) => s.remove());
    }
    normalizeColors(svg) {
        if (!this.color) {
            return;
        }
        Array.from(svg.querySelectorAll('*')).forEach((el) => {
            this.normalizeElementColors(el);
        });
    }
    normalizeElementColors(el) {
        this.cleanInlineStyles(el);
        this.normalizeFillAttribute(el);
        this.normalizeStrokeAttribute(el);
        this.applyStrokeWidth(el);
    }
    cleanInlineStyles(el) {
        const style = el.getAttribute('style');
        if (!style) {
            return;
        }
        const cleaned = style
            .replace(/fill\s*:\s*[^;]+;?/gi, '')
            .replace(/stroke\s*:\s*[^;]+;?/gi, '');
        if (cleaned) {
            el.setAttribute('style', cleaned);
        }
        else {
            el.removeAttribute('style');
        }
    }
    normalizeFillAttribute(el) {
        const fill = el.getAttribute('fill');
        if (fill && fill !== 'none' && !fill.startsWith('url(')) {
            el.setAttribute('fill', 'currentColor');
        }
    }
    normalizeStrokeAttribute(el) {
        const stroke = el.getAttribute('stroke');
        if (stroke && stroke !== 'none' && !stroke.startsWith('url(')) {
            el.setAttribute('stroke', 'currentColor');
        }
    }
    applyStrokeWidth(el) {
        if (this.strokeWidth != null && el.hasAttribute('stroke')) {
            el.setAttribute('stroke-width', String(this.strokeWidth));
        }
    }
    configureSvgAttributes(svg) {
        svg.removeAttribute('width');
        svg.removeAttribute('height');
        if (!svg.getAttribute('viewBox')) {
            svg.setAttribute('viewBox', `0 0 ${this.iconSize} ${this.iconSize}`);
        }
        svg.setAttribute('preserveAspectRatio', 'xMidYMid meet');
        svg.setAttribute('focusable', 'false');
        svg.setAttribute('aria-hidden', 'true');
        svg.setAttribute('width', '100%');
        svg.setAttribute('height', '100%');
        if (svg.style) {
            svg.style.display = 'block';
            svg.style.maxWidth = '100%';
            svg.style.maxHeight = '100%';
        }
        else {
            let style = svg.getAttribute('style') || '';
            style += ';display:block;max-width:100%;max-height:100%;';
            svg.setAttribute('style', style);
        }
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "19.2.19", ngImport: i0, type: FNIconComponent, deps: [{ token: i1.HttpClient }, { token: i0.ChangeDetectorRef }], target: i0.ɵɵFactoryTarget.Component });
    static ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "19.2.19", type: FNIconComponent, isStandalone: true, selector: "fn-icon", inputs: { name: "name", variant: "variant", size: "size", color: "color", strokeWidth: "strokeWidth", disabled: "disabled", className: "className", basePath: "basePath" }, viewQueries: [{ propertyName: "iconContainer", first: true, predicate: ["iconContainer"], descendants: true, static: true }], usesOnChanges: true, ngImport: i0, template: `
    <span
      #iconContainer
      [class]="className"
      [style.width.px]="iconSize"
      [style.height.px]="iconSize"
      [style.display]="'block'"
      [style.overflow]="'hidden'"
      [style.color]="color ? color : null"
      [style.opacity]="disabled ? 0.5 : 1"
      [style.cursor]="disabled ? 'not-allowed' : 'inherit'"
    ></span>
  `, isInline: true, dependencies: [{ kind: "ngmodule", type: CommonModule }] });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "19.2.19", ngImport: i0, type: FNIconComponent, decorators: [{
            type: Component,
            args: [{
                    selector: 'fn-icon',
                    standalone: true,
                    imports: [CommonModule],
                    template: `
    <span
      #iconContainer
      [class]="className"
      [style.width.px]="iconSize"
      [style.height.px]="iconSize"
      [style.display]="'block'"
      [style.overflow]="'hidden'"
      [style.color]="color ? color : null"
      [style.opacity]="disabled ? 0.5 : 1"
      [style.cursor]="disabled ? 'not-allowed' : 'inherit'"
    ></span>
  `,
                }]
        }], ctorParameters: () => [{ type: i1.HttpClient }, { type: i0.ChangeDetectorRef }], propDecorators: { name: [{
                type: Input
            }], variant: [{
                type: Input
            }], size: [{
                type: Input
            }], color: [{
                type: Input
            }], strokeWidth: [{
                type: Input
            }], disabled: [{
                type: Input
            }], className: [{
                type: Input
            }], basePath: [{
                type: Input
            }], iconContainer: [{
                type: ViewChild,
                args: ['iconContainer', { static: true }]
            }] } });

/**
 * Generated bundle index. Do not edit.
 */

export { FNIconComponent };
//# sourceMappingURL=arpudhabotupload-fn-icon-angular.mjs.map
